"use client"
import { Box, Text } from '@zuzjs/ui';
import React from 'react';

const Page : React.FC = (_props) => {
    return <Box as={`p:0,25`}>
       <Text>App Detail</Text>
    </Box>
}

export default Page;